// 01. Create variables that store the balance, deposit, and withdrawal values of a bank account, then perform addition and subtraction operations between them using a variable named 'operation', considering the function of each previously created variable. Display the final results on the console.

let balance = 1000;
const deposit = 200;
const withdrawal = 60;
const operation = (balance + deposit) - withdrawal;

console.log("Balance before operation: ", balance);
console.log("balance after operation: ", operation);

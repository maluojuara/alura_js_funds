///01. Declare three different variables (one for each type: string, number and boolean) and assign values ​​to them. Then display the type of each variable in the console.

const name = 'Malu'; // String
const age = '25'; // Number
const brazilian = true; // Boolean

if (brazilian == true){
    console.log(name);
    console.log(age);
}



// 02. "Create a Node.js program that starts with an array of five elements and assigns a new value to the first position (index 0) of this array. Then, display the array before and after the modification."

const array = [0, 1, 2, 3, 4];

console.log(array);

array[0] = 0.5;

console.log(array);
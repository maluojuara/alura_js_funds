const students = ['João', 'Juliana', 'Caio', 'Ana'];
const grades = [10, 8, 7.5, 9];

const list = [students, grades];

console.log(`Student at position 1 of list is: ${list[0][1]}. 
Grade of this student is: ${list[1][1]}`);
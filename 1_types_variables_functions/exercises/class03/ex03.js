// 03. Consider a situation where you are checking if a user is logged in and has administrator permission to access a particular functionality. Use boolean variables to simulate these conditions and use the AND operator to check if both are true.

const logged = true;
const admin = false;

const result = logged && admin;

console.log("Administrator logged: ", result); 
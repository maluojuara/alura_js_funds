const estudante = require('./estudante.json'); // built-in function

// console.log(estudante);
// console.log(typeof(estudante)); // returns object

const chaves = Object.keys(estudante);

console.log(chaves);
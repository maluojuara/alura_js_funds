//02. Create two variables, one containing your first name and one containing your last name. Then combine them into a third variable using the + operator and into a fourth variable using template strings. Finally, print the results obtained on the console.

const firstName = 'Malu';
const lastName = 'Araujo' 


// Using operator +
const fullNameOperator = firstName + ' ' + lastName;
console.log(fullNameOperator);

// Using template strings

const fullNameTemplate = `${firstName} ${lastName}`;
console.log(fullNameTemplate);
// 04. Declare two boolean variables and use the OR operator to check if at least one of them is true. Display the result on the console.

const variable1 = true;
const variable2 = false;

const result = variable1 || variable2;

console.log(result);
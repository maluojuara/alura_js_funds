// 04. Develop a program in Node.js that creates an empty array and assigns values to it using the push() method. Add three integers to the array and then display the resulting array.

const myArray = [];

myArray.push(90, 76, -2);

console.log(myArray);
// 03. Declare two variables, one containing a number and another containing a string. Then, combine them into a third variable using template strings to assemble a sentence, and display the result in the console.

const number = 25;
const string = "Malu Araujo";

const text = `Hi, my name is ${string} and I am ${number} years old`;
console.log(text);